$kind: http-request
url: "{{base_url}}/usuarios"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('401 sin token', function () { pm.response.to.have.status(401); });
    language: text/javascript
order: 5000
